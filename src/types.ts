export type Action = { label: string; href?: string };
export type Product = { name: string; detail: string; price: string; compareAt?: string; badge?: string; tone: string };
export type Tile = { title: string; eyebrow?: string; action?: string; tone: string };
